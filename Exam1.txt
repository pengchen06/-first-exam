1、java实现多线程的 方法：一、继承Thread类 二、实现Runnable接口
2、在需要同步的代码块前加上关键字：synchronized
3、list：元素有序，且不唯一。主要用于元素可以重复的场景或者对顺序有要求的
      set：元素无序，且元素唯一，主要用于元素不能重复场景
      map ：用键值对来存储数据，其中键必须唯一，值可以不唯一
   其中：list和set都是collection的子类
4、不晓得.....